<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bookstoreapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.FullWhite"
        tools:replace="android:allowBackup">

        <activity android:name=".author.AuthorBooksActivity" />
        <activity android:name=".userQuotes.UserQuoteDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".news.NewsDetailActivity" />
        <activity android:name=".bookQuotes.BookQuoteDetailActivity" />
        <activity android:name=".user.UserProfileActivity" />
        <activity android:name=".menu.AppSettingsActivity"
            android:noHistory="true"
            />
        <activity android:name=".menu.AppInfoActivity" />
        <activity android:name=".auth.LoginActivity"></activity>
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".BookReaderActivity" />
        <activity android:name=".print.PrintBooksActivity" />
        <activity android:name=".books.BookDetailActivity" />
        <activity android:name=".auth.EmailRecoveryActivity" />
    </application>

</manifest>